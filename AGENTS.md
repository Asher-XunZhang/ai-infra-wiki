# AGENTS.md

本仓库用于沉淀 AI Infra 相关学习资料。新增或整理文档时，优先产出可长期阅读、可 diff、可复用的 Markdown，而不是临时摘要。

本仓库的学习资料默认分成两类，方法论也分开执行：

1. **源码分析型学习资料**：从某个代码库的源码梳理系统机制，例如 `sglang/PD 分离下的 PP 源码学习文档.md`。
2. **第三方资料整理型学习资料**：从文章、网页、PDF、博客、图文资料等外部资料整理学习笔记，例如 Mooncake 相关文章整理。

无论哪一类，都要先读同目录已有文档风格，优先参考 `sglang/PD 分离下的 PP 源码学习文档.md` 的“小白可读、机制完整、图文结合”节奏。

## 0. 文档落盘与目录组织

所有分析文档在创建或整理前，agent 都必须先查看仓库现有目录和相关文档，自行判断结果最适合放在哪个目录，不能未经判断就默认放在仓库根目录或当前目录。

目录组织遵循以下要求：

- 优先按技术主题、项目、系统或资料主线归类，尽量把相关性高、需要相互参照或适合连续阅读的文档放在同一目录。
- 如果现有目录无法清楚承载新文档，可以创建新的主题目录；必要时也允许在合理范围内重新调整整个仓库的目录架构，使其更便于长期阅读、检索和维护。
- 目录调整应服务于内容关系，不为单篇文档制造无意义的深层目录，也不要按临时任务名、抓取平台或一次性处理方式分类。
- 仓库内所有图片文件必须统一存放在仓库根目录的顶层 `images/<topic>/` 下。这里的 `images/` 固定指与 `AGENTS.md` 同级的 `<repo-root>/images/`；禁止在 `sglang/`、`vllm/`、`llm-inference/` 或其他内容目录内另建 `images/`、`image/`、`assets/` 来存放文档图片。
- 移动或重命名文档、图片、目录后，必须搜索并同步更新仓库内所有相关引用，包括 Markdown 相对链接、图片路径、目录索引、README、示例路径和其他文字引用，不能留下失效路径。
- 完成目录调整后，必须验证受影响的相对链接和图片路径仍能从引用文档的位置正确解析，并通过 `git status --short` 确认变更范围符合本次任务。

## 1. 源码分析型学习资料

### 适用场景

当资料来源是一个代码库、某个分支、某组源码文件或一次源码阅读任务时，使用本方法论。目标不是列函数清单，而是把源码里的控制流、数据流、状态机、对象生命周期和边界条件整理成第一次读代码的人也能看懂的学习文档。

典型参考：`sglang/PD 分离下的 PP 源码学习文档.md`，它来自对 `/Users/mac/Documents/Documents/工作/sglang` 中 `muxi-main` 分支的 SGLang 源码做只读梳理。

### 必须先固定源码基线

源码文档开头必须写清楚：

| 项目 | 要求 |
| --- | --- |
| 源码目录 | 使用绝对路径，说明实际读取的是哪个代码库 |
| 分支 | 写明分支名，例如 `muxi-main` |
| commit | 写明 commit hash |
| 读取时间 | 使用 `YYYY-MM-DD` |
| 工作区状态 | 说明是否存在未跟踪文件、本地改动或脏工作区 |
| 操作边界 | 说明是只读梳理，还是包含实验、patch、运行验证 |

如果只是读源码，不要暗示已经跑过实验。若源码仓库有本地改动，只记录状态，不要擅自回滚、清理或覆盖。

### 推荐阅读流程

源码阅读不要从单个函数硬啃，按下面顺序建立全局地图：

1. **入口选择**：找启动入口、模式分发、核心 event loop、CLI 参数处理或主要 manager。
2. **主线确认**：明确本文只讲哪条主线，例如“PD 分离下的 PP 行为”，并说明不展开哪些路径。
3. **对象盘点**：列出请求、batch、queue、rank、stage、connector、cache、metadata 等核心对象。
4. **控制流追踪**：按请求从进入系统到释放资源的顺序追踪函数调用。
5. **数据流追踪**：单独追踪 tensor、KV cache、metadata、网络消息、控制消息等真正流动的数据。
6. **状态机整理**：把 queue 状态、poll 状态、ready/release 条件、batch 生命周期整理成表格或图。
7. **硬约束整理**：记录启动参数限制、assert、兼容性开关、合法拓扑和失败回退。

每深入一个函数，都要回答“它在整条链路中负责哪一步”。不要只罗列源码片段。

### 源码锚点写法

源码学习文档必须让读者能回到代码：

- 用表格列出行为和对应文件/函数，例如“根据 PD mode 选择 PP event loop -> `scheduler.py::run_scheduler_process`”。
- 代码片段只摘最关键的分支、状态更新或数据结构，不整段搬运。
- 文件路径、函数名、类名、枚举名要准确；不确定时重新 `rg` 或打开源码确认。
- 对代码片段后的解释先讲行为，再讲为什么这样设计。
- 源码事实、运行观察、整理者推断必须区分；推断用“可以理解为”“这里的作用是”等表述。

### 面向小白的表达要求

源码型学习文档要主动降低阅读门槛。每个复杂章节尽量包含：

- **人话版**：不用源码名先解释机制。
- **术语速查**：把缩写、rank、stage、queue、cache、connector 讲成普通语言。
- **源码锚点**：告诉读者去哪里看真代码。
- **流程图或状态图**：优先用 Mermaid 表达控制流、数据流、状态机和拓扑。
- **例子或比喻**：用一条请求、一个 microbatch、一个 rank 拓扑走完整流程。
- **排障地图**：把现象反查到可能的状态、函数或对象边界。

尤其要拆开同名但不同生命周期的对象，例如 proxy tensor、KV cache、控制消息；也要拆开“能读写某 buffer”和“拥有调度/生命周期策略”。

### 源码型文档推荐结构

```markdown
# 某源码主题学习文档

本文面向第一次读该源码主题的同学，只研究某条明确主线。

## 0. 阅读基线与范围

**源码基线**

| 项目 | 内容 |
| --- | --- |
| 源码目录 | ... |
| 分支 | ... |
| commit | ... |
| 读取时间 | ... |
| 工作区状态 | ... |

**术语速查**

| 术语 | 人话解释 | 主要源码 |
| --- | --- | --- |
| ... | ... | ... |

## 1. 先建立整体地图

### 人话版
### 源码锚点
### 总览图
### 例子

## 2. 启动参数和硬约束

## 3. 核心对象与状态机

## 4. 主流程 walkthrough

## 5. 数据流和控制流拆解

## 6. 小白排障地图

## 7. 源码阅读路线

## 8. 一句话总结
```

### 源码型验证清单

写完源码学习文档后至少检查：

- 文档开头是否记录源码目录、分支、commit、读取时间和工作区状态。
- 主线范围是否明确，未覆盖路径是否清楚声明。
- 每个关键行为是否有源码锚点。
- Mermaid 图是否和文字描述一致。
- 是否区分源码事实、运行观察和整理者推断。
- 是否没有修改被阅读的外部源码仓库，除非用户明确要求。
- 用 `rg` 检查标题层级，必要时抽查路径和函数名仍存在。

## 2. 第三方资料整理型学习资料

### 适用场景

当资料来源是文章、网页、PDF、博客、图文资料、报告、官方文档或第三方说明时，使用本方法论。目标不是复述原文，而是把资料中的技术主线、图片信息、结论边界和可迁移理解整理成学习文档。

### 必须先分析来源

动笔前先提取并记录：

| 项目 | 要求 |
| --- | --- |
| 原文标题 | 保留完整标题 |
| 原文链接 | 保留可追溯 URL 或文件路径 |
| 作者/机构 | 有则记录 |
| 发布时间/读取时间 | 没有发布时间时至少记录读取时间 |
| 资料类型 | 文章、官方文档、论文、图文、报告等 |
| 整理范围 | 本文讲什么，不讲什么 |
| 验证边界 | 是否只基于原文整理，是否做了源码复核或实验复现 |

正文应以学习笔记和二次理解为主，避免整段搬运原文。关键技术判断要区分“原文信息”和“整理者归纳”。

### 图片资料处理流程

第三方资料里的图片必须先理解再嵌入：

1. 提取所有重要图片。
2. 判断每张图表达的是架构、数据流、控制流、性能结果、层级抽象还是装饰。
3. 保留有信息量的原图，下载到仓库根目录的顶层 `images/<topic>/`。
4. 使用相对路径嵌入文档。
5. 每张关键图下写原创“图意解读”，说明图里谁拥有控制权、谁负责数据面、边界在哪里。
6. 如果图片是性能图，必须说明结果只适用于原文给定条件，不能泛化。

不要只把图片当插图，也不要只引用远程图片链接。

### 第三方资料推荐结构

```markdown
# 某主题学习文档

本文面向第一次了解该主题的同学，整理某资料的图文/文档内容。本文是学习笔记；若未做源码级复核或实验复现，需要在这里说明。

## 0. 阅读基线与范围

| 项目 | 内容 |
| --- | --- |
| 原文标题 | ... |
| 原文链接 | ... |
| 作者/机构 | ... |
| 读取时间 | YYYY-MM-DD |
| 整理范围 | ... |
| 不展开内容 | ... |

**术语速查**

| 术语 | 人话解释 | 在本文中的重点 |
| --- | --- | --- |
| ... | ... | ... |

## 1. 先建立整体地图

### 人话版
### 总览图
### 图意解读
### Mermaid 版控制图
### 小例子

## 2. 核心机制拆解

### 人话版
### 机制拆解
### 图意解读
### 例子或排障提示

## N. 一句话总结

## N+1. 参考与延伸
```

### 第三方资料验证清单

写完第三方资料整理文档后至少检查：

- 是否写明来源、作者/机构、读取时间和整理边界。
- 是否保留原文链接或文件路径。
- 是否明确“基于原文整理”与“整理者归纳”的边界。
- 每张重要图片是否有图意解读。
- 图片是否已下载到本地并使用相对路径。
- 是否还残留不应依赖的远程图片链接。
- 性能数字、benchmark、经验结论是否写明适用条件。

## 3. 共用写作规则

无论是源码分析型还是第三方资料整理型，每章尽量围绕四件事展开：

- **人话版**：先把复杂机制翻译成普通语言。
- **机制拆解**：说明控制权、数据流、生命周期、接口边界。
- **图意解读**：解释图中每个组件在系统里的角色。
- **例子或排障提示**：把抽象概念拉回具体请求、配置或故障现象。

共用边界：

- 不把未验证的来源内容写成“当前事实”。
- 不把性能图中的数字泛化为通用结论。
- 不把“能访问某资源”和“拥有该资源的调度/生命周期策略”混为一谈。
- 不为了凑格式强行加章节；结构要服务读者理解。
- 不把平台名、抓取方式或临时处理方式写进新建文件/目录名称，除非它们是技术主题本身。

## 4. 图片和图表规则

### Mermaid 使用规则

- 能用 Mermaid 清楚表达控制流、状态机、时序或简单架构时，优先使用 Mermaid，方便 diff 和维护。
- 源码型文档推荐使用：
  - `flowchart`：控制流、数据流、模块关系。
  - `sequenceDiagram`：请求跨组件、跨 rank、跨 P/D 的时序。
  - `stateDiagram`：queue、poll、batch、request 生命周期。
- Mermaid 图必须和正文描述一致，不要为了好看添加源码或资料里没有的模块。

### 本地图片规则

- 图片应下载到仓库根目录的顶层 `images/<topic>/` 目录，并使用相对路径嵌入文档；例如 `vllm/foo.md` 应引用 `../images/<topic>/01-overview.png`。
- 不允许在文档分类目录下维护第二套图片根目录，例如 `vllm/images/`、`sglang/images/` 或 `llm-inference/assets/`。发现这类目录时，应把图片迁移到 `<repo-root>/images/<topic>/`，更新全部引用，并删除空目录。
- `<topic>` 使用中性、长期可读的主题名，例如 `mooncake-articles`，避免把来源平台写进目录名。
- 图片文件名用有含义的英文短名，例如 `01-overview.png`、`02-control-flow.png`、`03-benchmark.png`。
- 文档中不要依赖远程图片链接；保留原文链接即可。
- 对透明背景图片，要检查在常见 Markdown 预览背景下是否可读；必要时保留原图并在文字里提示图意。

### 非 Mermaid 图片生成规则

- 当图需要表达复杂视觉层级、原图重绘、空间布局、对照海报、性能示意或 Mermaid 难以清晰呈现的关系时，可以生成 PNG/SVG 等非 Mermaid 图片。
- 生成图必须放在仓库根目录的顶层 `images/<topic>/` 下，文件名使用英文短名并带序号，例如 `05-rank-topology.png`。
- 生成图不能替代原始技术图的证据作用。若来源资料已有关键原图，应先保留原图，再按需要额外生成“整理版/重绘版”。
- 文档中必须说明生成图的用途，例如“整理者重绘：用于突出数据流，不代表源码中的真实模块边界”。
- 如果生成图来自 AI 绘图或手工重绘，应避免引入原文没有的信息；推断内容要在图下文字中标注为整理者归纳。
- 不为纯装饰生成图片。学习文档里的非 Mermaid 图片应服务于理解机制、拓扑、数据流、性能对比或排障路径。
- 生成图后要检查尺寸、文字可读性、透明背景、移动端预览和相对路径，确认它在常见 Markdown 预览中能直接阅读。

## 5. 最终验证清单

新增或修改学习资料后至少检查：

```bash
rg -n "^(#|##|###|####) " <doc.md>
```

- 标题层级是否清晰。
- 文档类型是否明确属于“源码分析型”或“第三方资料整理型”。
- 是否已根据主题相关性判断文档落盘目录，并尽量与高相关文档归在同一目录。
- 是否写明来源、读取时间和整理边界。
- 所有本地图片相对路径是否能从文档位置解析到真实文件。
- 是否用全仓扫描确认图片文件只存在于 `<repo-root>/images/`，且 `sglang/`、`vllm/`、`llm-inference/` 等内容目录下没有残留图片文件或图片根目录。
- 若移动或重命名了文件、图片或目录，是否已搜索并更新 README、索引、文档链接、图片路径和其他文字引用。
- 是否还残留不应依赖的远程图片链接。
- 是否每张重要图片都有图意解读，而不是只有图片。
- 是否区分事实、观察和整理者推断。
- `git status --short` 中只出现本次任务相关文件。
